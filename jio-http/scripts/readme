NAME="jio-http"
VERSION="1.0.0-RC3"
IMAGE="${NAME}:${VERSION}"


//go to root folder and build the image
jio-http % docker build -t ${IMAGE} .

//go to script and execute the command specifying the version
scripts % ./jio-http-compile.sh --version ${VERSION}