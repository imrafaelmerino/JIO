NAME="jio-exp"
VERSION="1.0.0-RC3"
IMAGE="${NAME}:${VERSION}"


//go to root folder and build the image
jio-exp % docker build -t ${IMAGE} .

//go to script and execute the command specifying the version
scripts % ./jio-exp-compile.sh --version ${VERSION}