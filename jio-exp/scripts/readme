NAME="jio-exp"
VERSION="1.2.0"
IMAGE="${NAME}:${VERSION}"


//go to root folder jio-exp

docker build -t ${IMAGE} .
./scripts/jio-exp-compile.sh --version ${VERSION}
./scripts/jio-exp-package.sh --version ${VERSION}